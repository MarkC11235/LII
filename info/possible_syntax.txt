Calling functions and accessing objects should be able to be done anywhere.

print func(x, y){return x + y;}(1, 2);

or 

print [1, 2, 3][0];

or

let f_maker = func() {
    let f = func(x) {
        return x + 1;
    };
    return f;
};

print f_maker()(1);

####################################################