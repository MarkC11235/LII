#ifndef MISC_HPP
#define MISC_HPP

#include <iostream>
#include <cstdlib>

int exit_program(std::string message);

int wait(double seconds);

int system_command(std::string command);

#endif // MISC_HPP